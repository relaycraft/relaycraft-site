---
import Layout from '../../layouts/Layout.astro';
import { Header } from '../../components/Header';
import { Footer } from '../../components/Footer';
import { ui } from '../../i18n/ui';

export function getStaticPaths() {
  return [
    {params: {lang: 'en'}},
    {params: {lang: 'zh'}},
  ];
}

const { lang } = Astro.params;
const currentLang = (lang as 'en' | 'zh') || 'en';
const content = ui[currentLang];
---

<Layout title={`${content['terms.title']} - RelayCraft`} description={content['terms.intro']} lang={currentLang}>
  <Header client:load lang={currentLang} content={content} />
  
  <main className="pt-32 pb-20">
    <div className="container px-4 md:px-6 mx-auto max-w-3xl">
      <header className="mb-12">
        <h1 className="text-4xl md:text-5xl font-bold mb-6 text-foreground">
          {content['terms.title']}
        </h1>
        <p className="text-sm font-medium text-muted-foreground">
          {content['terms.lastUpdated']}
        </p>
      </header>

      <div className="prose prose-lg dark:prose-invert max-w-none">
        <p className="lead text-xl text-muted-foreground font-medium mb-10">
          {content['terms.intro']}
        </p>

        <section className="mb-10">
          <h2 className="text-2xl font-bold mb-4 text-foreground">{content['terms.license.title']}</h2>
          <p>{content['terms.license.content']}</p>
        </section>

        <section className="mb-10">
          <h2 className="text-2xl font-bold mb-4 text-foreground">{content['terms.usage.title']}</h2>
          <p>{content['terms.usage.content']}</p>
        </section>

        <section className="mb-10">
          <h2 className="text-2xl font-bold mb-4 text-foreground">{content['terms.warranty.title']}</h2>
          <p>{content['terms.warranty.content']}</p>
        </section>
      </div>
    </div>
  </main>
  
  <Footer lang={currentLang} content={content} />
</Layout>
